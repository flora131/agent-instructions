{
	"name": "atomic-extension-gondolin",
	"private": true,
	"version": "0.79.7",
	"type": "module",
	"scripts": {
		"clean": "echo 'nothing to clean'",
		"build": "echo 'nothing to build'",
		"check": "tsgo -p ../../../test/tsconfig.remote-examples.json"
	},
	"dependencies": {
		"@earendil-works/gondolin": "0.12.0"
	},
	"atomic": {
		"extensions": [
			"./index.ts"
		]
	}
}
